# services:
#   back-end:
#     build:
#       context: ./back-end
#     ports:
#       - "3000:3000"

#   frontend:
#     build:
#       context: ./frontend
#     ports:
#       - "5173:3000"
#     depends_on:
#       - back-end

services:
  back-end:
    build:
      context: ./back-end
    ports:
      - "8443:3636"
    environment:
      - PORT=3636
      - OCR_SERVICE_URL=http://ai-service:8000
    volumes:
      - ./back-end:/app
      - /app/node_modules
    depends_on:
      - ai-service

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - back-end
    volumes:
      - ./frontend:/app
      - /app/node_modules

  ai-service:
    build: ./ai-services
    ports:
      - "8000:8000"
    restart: on-failure
